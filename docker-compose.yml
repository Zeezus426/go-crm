version: 'latest'

services:
  database:
    image: "postgres:latest"
    ports:
      - "1111:1111"
    environment:
      POSTGRES_USER: "metro-user"
      POSTGRES_PASSWORD: "its_terrible"
      POSTGRES_DB: "458speciale"